# core/db.py — addition only (do NOT overwrite your existing db.py)

Your existing `core/db.py` needs ONE new function appended to it (it does
not touch or modify anything that already exists in that file). Copy the
code below from `db_py_addition_snippet.py` in this folder and paste it
onto the END of your existing `core/db.py`.
